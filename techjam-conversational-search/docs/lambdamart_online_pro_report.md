# LambdaMART + DeepSeek Pro 在线评测

[测试与产物入口](../../docs/TESTING.md) · [完整归档](../evaluation_runs/lambdamart_online_pro_200/lambdamart/20260830_211751_+0800/README.md)

本次有效结果只包含新的 LambdaMART + deepseek-v4-pro，完整运行官方200条。原精排直接引用已有历史结果，没有把已取消的重复运行计入比较。

| 指标 | 历史原精排 Pro | 本次 LambdaMART Pro |
|---|---:|---:|
| 会话 Hit@10 | 92.5%（185/200） | 97.0%（194/200） |
| MRR | 0.469921 | 0.511349 |
| MTTC（失败记11） | 3.095 | 2.295 |
| TechnicalScore | 0.761576 | 0.812505 |

两次使用相同的200条场景、相同商品目录和 deepseek-v4-pro。历史记录存在未提交源码修改，并使用6个进程，本次使用4个进程；LLM输出也并非严格确定。因此上述数字用于历史参考，不能据此把所有差异都归因于精排替换。

## 本次实际执行

- 直接复用 scripts/evaluate_parallel_with_traces.py → scripts/evaluate_with_traces.py → 原有模拟器及评分规则。
- 显式指定 --model deepseek-v4-pro、--ltr-ranker lambdamart、--candidate-limit 0。
- LLM负责需求解析与对话决策；用户仍由本地模拟器生成，召回后端为local。
- 使用已训练的167棵树模型，没有在线训练、测试集调参或默认精排切换。
- 模型SHA256：d4243775f26f8fc5b651becd0100d6a69d232401b73b7371f1c9e0bc4f72b79a。
- 数据、模型和执行代码指纹全部复核通过。
- 运行约719秒；精排本身中位约12.31ms，p95约22.56ms。不与历史不同并发条件作严格延迟比较。

## Trace 核查

- 200条会话，453个轮次。
- 4,959条节点记录，1,750份完整候选池快照；所有已写出的候选快照均核对count与实际长度一致。
- 451份实际精排输入/输出，最多500个候选，逐候选保存13个特征、分数和排序。
- 904次SDK调用均收到响应，返回模型全部为deepseek-v4-pro。每次请求和响应按sample_id、turn、call_id关联。
- 9个异常轮次：2次需求解析失败、7次对话输出校验失败。它们保留在评测中，无离线兜底，也未筛掉重跑。
- 需求解析失败的2轮没有进入精排，因此排序记录比总轮数少2条；这不是trace丢失。
- 原始SDK用量为1,145,176 tokens；Agent报表为1,125,187。差额来自异常轮次未返回到Agent usage字段的调用，原始调用记录保留了这些用量。
- 不记录认证头或密钥。SDK内部HTTP重试不单独展开；这里的904是SDK调用数。
- rank_calls.jsonl中的其他排序器分数仅在同一输入上做本地计算用于核查，不会额外调用LLM或重跑对照会话。

## 文件位置

完整运行目录（相对于 `techjam-conversational-search/`）：
`evaluation_runs/lambdamart_online_pro_200/lambdamart/20260830_211751_+0800/`。
下面三份大日志以 `.jsonl.gz` 无损压缩保存，表中的大小为解压后大小。

| 文件 | 内容 | MiB |
|---|---|---:|
| sessions.jsonl | 每条样本的结果、目标、画像、错误 | 0.51 |
| turns.jsonl | 每轮输入、回复、需求状态、推荐、用量与耗时 | 0.88 |
| node_traces.jsonl | 完整节点更新和候选池 | 388.66 |
| llm_calls.jsonl | LLM请求、原始响应、用量与异常 | 5.48 |
| rank_calls.jsonl | 全部候选的特征、分数和真实排序 | 32.08 |
| trace.json | 现有trace查看器使用的便携摘要 | 4.35 |
| trace_audit.json | 完整性核查结果 | 0.00 |

trace.json用于查看，完整候选/LLM请求应以JSONL为准。
`evaluation_runs/lambdamart_online_pro_200/result.json` 保存本次结果与历史参考出处。

## 执行纠正记录

执行过程中误启动了Flash，随后又重复启动了已有的原精排Pro。用户纠正后均已停止，原工作区的 trace 当时标记为 CANCELLED；它们不参与本次结果，也不在 final 的保留归档内。线性对照没有启动。

- 已取消Flash：53条完成会话，已返回日志记录324,440 tokens。
- 已取消重复原精排Pro：121条完成会话，已返回日志记录838,286 tokens。
- 在途请求取消时可能尚未返回用量，以上取消用量不是精确账单。
- 多余的experiment_lambdamart_online.py入口已删除，后续直接使用原有并行脚本。

历史原精排参考的运行 ID 为 `main_full_trace_pro_200/20260830_154054_+0800`；
这里只保留比较出处，不再将已清理的旧目录写作可用入口。


## 仓库归档

[本次完整结果与解压说明](../evaluation_runs/lambdamart_online_pro_200/lambdamart/20260830_211751_+0800/README.md)。节点、LLM调用和精排日志以无损 gzip 保存，并附 SHA256 清单；其余会话、逐轮结果和查看器 trace 原样保留。已取消运行不上传。

## 六条未命中样本分析

只读取本次 trace，并在同一候选上检查已保存的对照分数；没有重跑评测或调用 LLM。

| 样本 | 证据 | 判断 |
|---|---|---|
| public_0029 | 目标始终进入精排，最好第24名；需求一直是 fabric sandals flats。第4轮 reason 为308字符，超过300字符限制。 | 需求区分度不足、目标未进Top10；另有一轮输出校验错误，但不是唯一失败原因。 |
| public_0066 | 目标精排129→75；只有聚酯纤维正式裙需求，第10轮才问feature。 | 追问未及时取得更多信息，目标仍排在Top10之外。 |
| public_0073 | 前2轮未召回；第3—6轮属性召回216名、原始RRF融合559名，被Top500截断；第7轮权重切至buying后融合487名进入候选，精排39→30→22→14。 | 前期召回/融合截断，后期精排差4名。会话总标签不能代表全程。 |
| public_0096 | 10轮未召回；第3轮要求忽略旧偏好后，Pull On closure仍为硬约束。 | 召回失败，并有意图替换残留；目标也符合旧约束，不能断言残留直接导致失败。 |
| public_0161 | 10轮未召回；需求一直为cotton t-shirts and blouses。 | 召回阶段失败，精排没有处理目标的机会；图案、节日信息未进入实际对话。 |
| public_0167 | 同候选第1轮：旧排序62、线性57、树141；第10轮：22、14、79。 | 本样本树模型明显退步，但三个排序器在这些实际输入上均未命中Top10。 |

0167第1轮TreeSHAP对目标与Top10平均分差的分解显示，评分质量特征贡献约2.981、RRF贡献约1.264，是该对比分差的主要来源。此为模型分数解释，不是修改特征后的因果实验。

共性：模拟用户只从有限需求卡回答，许多可披露内容被归到feature，而候选属性分析没有feature维度，追问经常得到“无额外偏好”。指定商品ID的命中失败也不等于所有推荐都不符合已表达需求；不能用隐藏目标信息补入对话来提高分数。建议先检查追问信息增益、意图替换和融合截断，再验证树模型对评分和上游分数的依赖。
